__include__: abstract.yaml
model:
  network:
    classeme:
      type: mfb
      input_dims: [100, 100]
      output_dim: 200
      mm_dim: 700
      factor: 5
      normalize: true
    spatial:
      type: mfb
      input_dims: [4, 4]
      output_dim: 200
      mm_dim: 700
      factor: 5
      normalize: true
    feature:
      type: mfb
      input_dims: [2048, 2048]
      output_dim: 200
      mm_dim: 700
      factor: 5
      normalize: true
